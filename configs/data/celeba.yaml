_target_: src.data.celeba_datamodule.CelebADataModule
data_dir: ${paths.data_dir}
batch_size: 32 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [162_770, 19_867, 19_962]
num_workers: 31
pin_memory: False